// Moved to scripts/smoke-test.mjs — kept as a redirect note so the old path
// doesn't surprise anyone.
console.log('This script moved. Run: node scripts/smoke-test.mjs <path-to-docx>');
process.exit(1);
